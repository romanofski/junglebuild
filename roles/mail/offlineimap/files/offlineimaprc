# Sample minimal config file.  Copy this to ~/.offlineimaprc and edit to
# suit to get started fast.

[general]
ui = Noninteractive.Quiet
# ui = Curses.Blinkenlights
# accounts = Changeme
maxsyncaccounts = 2

[Account Changeme]
localrepository = ChangemeLocal
remoterepository = ChangemeRemote
autorefresh = 5

[Repository ChangemeLocal]
type = Maildir
localfolders = ~/Maildir/

[Repository ChangemeRemote]
type = IMAP
ssl = yes
maxconnections = 3
# remotehost = mail.test.com
# remoteport = 993
# remoteuser = changeme@test.com
# remotepass = secret
# folderfilter = lambda x: x not in []
